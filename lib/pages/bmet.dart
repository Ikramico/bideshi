import 'package:flutter/material.dart';

class BMET extends StatelessWidget {
  const BMET({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('BMET'));
  }
}